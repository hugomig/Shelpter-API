{
  "name": "shelpterapi",
  "version": "1.0.0",
  "description": "api to access the data of mongodb with http from the app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.0",
    "express": "^4.17.1",
    "mongodb": "^3.6.3"
  }
}
